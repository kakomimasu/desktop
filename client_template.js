import { Algorithm } from "./algorithm.js";

export class #NAME# extends Algorithm {
  onInit(boardPoints, agentNum, turnNum) {

  }

  onTurn(field, pid, agents, turn) {

  }
}

if (import.meta.main) {
  let a = new #NAME#();
  a.match({
    id: "#NAME#",
    name: "#NAME#",
    spec: "",
    password: "#NAME#-pw"
  });
}
